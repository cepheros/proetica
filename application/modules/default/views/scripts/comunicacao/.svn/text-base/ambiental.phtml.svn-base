<section id="headline2">
<div class="container">
<h3>ProÉtica Ambiental
<small>Selecione a tipo de chamado desejado:</small></h3>
</div>
</section>
<section class="container page-content" >

<div class="vertical-space3"></div>

<p><span class="dropcap1">a</span> PróÉtica Ambiental é um canal de comunicações que tem como objetivo informar aos gestores do projeto situações de desvio de conduta de funcionários e colaboradores de empresas privadas e órgãos públicos e também sugestões de melhorias nas diversas atividades envolvidas pelos usuários do site PróÉtica</p> 
<p>Os formulários foram desenvolvidos para que o usuário apresente informações de forma clara e precisa e a PróÉtica Ambiental possa colaborar para que as providências cabíveis sejam alcançadas</p>

<div class="alert alert-error">
<button type="button" class="close" data-dismiss="alert">&times;</button>
<strong>Caso existam riscos de prejuízo patrimonial irreparável ou contra pessoas comunique imediatamente os fatos as autoridades locais, use o 190.</strong>
</div>

<div class="title">
<h4>Selecione o tipo de chamado:</h4>
</div>
<div class="vertical-space1"></div>

<?php foreach($this->tipos as $tipo) { ?>
<form id="chamado-<?php echo $tipo->id_registro?>" method="post" action="/comunicacao/criar">
<input type="hidden" name="idchamado" value="<?php echo $tipo->id_registro?>">
<span class="acc-trigger"><a href="#"><?php echo utf8_encode($tipo->titulochamado) ?></a></span>
<div class="acc-container">
<div class="content">
<p><?php echo utf8_encode($tipo->descritivochamado) ?></p>
<input type="submit" class="small green" name="submit" value="Abrir Chamado">
</div>
</div>
</form>
<?php } ?>




</section>

<script>

function abrirchamado(id){
	window.location = ""
}
$().ready(function(){

var $container = $('.acc-container'),
	$trigger   = $('.acc-trigger');

$container.hide();
//$trigger.first().addClass('active').next().show();

var fullWidth = $container.outerWidth(true);
$trigger.css('width', fullWidth);
$container.css('width', fullWidth);

$trigger.on('click', function(e) {
	if( $(this).next().is(':hidden') ) {
		$trigger.removeClass('active').next().slideUp(300);
		$(this).toggleClass('active').next().slideDown(300);
	}
	e.preventDefault();
});

// Resize
$(window).on('resize', function() {
	fullWidth = $container.outerWidth(true)
	$trigger.css('width', $trigger.parent().width() );
	$container.css('width', $container.parent().width() );
});

});



</script>
