<section id="headline2">
<div class="container">
<h3>Quiz PróÉtica</small></h3>
</div>
</section>

<section class="container" >
<div class="vertical-space1"></div>
<h3>Selecione o Quiz Desejado:</h3>
<?php foreach($this->quiz as $item){?>
<span class="acc-trigger"><a href="#"><strong><?php echo $item->nomequiz ?></strong></a></span>
<div class="acc-container">
<div class="content">
<p style="text-align: justify;"><?php echo $item->quizdescription ?></p>
<p style="text-align: justify;"><a href="/pesquisas/abrir/id/<?php echo $item->id_registro ?>">Abrir Quiz</a></p>
 
</div>
</div>
<?php }; ?>

</section>


<script>
$().ready(function(){

var $container = $('.acc-container'),
	$trigger   = $('.acc-trigger');

$container.hide();
$trigger.first().addClass('active').next().show();

var fullWidth = $container.outerWidth(true);
$trigger.css('width', fullWidth);
$container.css('width', fullWidth);

$trigger.on('click', function(e) {
	if( $(this).next().is(':hidden') ) {
		$trigger.removeClass('active').next().slideUp(300);
		$(this).toggleClass('active').next().slideDown(300);
	}
	e.preventDefault();
});

// Resize
$(window).on('resize', function() {
	fullWidth = $container.outerWidth(true)
	$trigger.css('width', $trigger.parent().width() );
	$container.css('width', $container.parent().width() );
});

});



</script>